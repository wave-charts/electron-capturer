{
  "name": "electron-video-recorder",
  "version": "0.0.5",
  "description": "Record Electron's window and export as video",
  "main": "./dist/index.js",
  "repository": "git@github.com:wave-charts/electron-capturer.git",
  "author": "wave-charts",
  "license": "MIT",
  "private": false,
  "keywords": [
    "electron",
    "capture",
    "record",
    "video",
    "record-video",
    "record-screen",
    "ffmpeg"
  ],
  "scripts": {
    "start:electron": "electron ./examples/main.js",
    "dev": "rimraf dist && tsc && yarn start:electron",
    "build": "rimraf dist && tsc"
  },
  "devDependencies": {
    "electron": "^15.3.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "debug": "^4.3.2",
    "fluent-ffmpeg": "^2.1.2",
    "fs-extra": "^10.0.0",
    "tslib": "^2.3.1"
  }
}
